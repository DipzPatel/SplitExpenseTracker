<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SplitExpenseTracker">
        <activity
            android:name=".ui.group.GroupActivity"
            android:exported="true"
            android:theme="@style/Theme.SplitExpenseTracker.Splash">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <activity
            android:name=".ui.member.MemberActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.expense.ExpenseActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.balance.BalanceActivity"
            android:exported="false" />

        <activity
            android:name=".ui.settlement.SettlementActivity"
            android:exported="false" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"/>

    </application>

</manifest>